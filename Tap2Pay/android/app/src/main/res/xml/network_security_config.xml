<?xml version="1.0" encoding="utf-8"?>
<!--
  network_security_config.xml — TLS certificate pinning for the merchant terminal app.

  PINNING STRATEGY:
  We pin to the ISRG Root X1 CA (Let's Encrypt's root CA) rather than leaf certs.
  This survives certificate renewals (every 90 days for LE certs) without an app update,
  while still preventing MITM attacks from other CAs. The ISRG Root X1 pin is stable
  across all Let's Encrypt-issued certificates.

  BACKUP PIN:
  The ISRG Root X2 (Let's Encrypt's ECDSA root) is the backup. If ISRG Root X1 is
  ever retired, the backup pin keeps connections alive during the transition.

  TO EXTRACT PINS FOR A CUSTOM CA (run before switching TLS provider):
    openssl s_client -connect api.orchestratepay.co.ke:443 </dev/null 2>/dev/null \
      | openssl x509 -pubkey -noout \
      | openssl pkey -pubin -outform der \
      | openssl dgst -sha256 -binary \
      | base64

  Or use the helper script: scripts/extract-tls-pin.sh

  WARNING: A wrong primary pin with no valid backup breaks ALL release-build connections.
  Always test pin changes on a device before shipping a release APK.
-->
<network-security-config>

    <!-- Debug builds: trust user-installed CAs (Charles Proxy, mitmproxy, Burp) -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <!-- Production API: pin to ISRG Root CA (Let's Encrypt) -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.orchestratepay.co.ke</domain>
        <pin-set expiration="2028-01-01">
            <!-- PRIMARY: ISRG Root X1 (RSA 4096) — SHA-256 of SubjectPublicKeyInfo -->
            <pin digest="SHA-256">C5+lpZ7tcVwmwQIMcRtPbsQtWLABXhQzejna0wHFr8M=</pin>
            <!-- BACKUP: ISRG Root X2 (ECDSA P-384) — Let's Encrypt ECDSA root -->
            <pin digest="SHA-256">diGVwiVYbubAI3RW4hB9xU8e/CH2GnkuvmFZRIjnadY=</pin>
        </pin-set>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!-- Safaricom Daraja API: pin to Safaricom's intermediary (DigiCert) -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">sandbox.safaricom.co.ke</domain>
        <domain includeSubdomains="true">api.safaricom.co.ke</domain>
        <pin-set expiration="2028-01-01">
            <!-- DigiCert Global Root CA — used by Safaricom -->
            <pin digest="SHA-256">r/mIkG3eEpVdm+u/ko/cwxzOMo1bk4TyHIlByibiA5E=</pin>
            <!-- DigiCert Global Root G2 — backup -->
            <pin digest="SHA-256">i7WTqTvh0OioIruIfFR4kMPnBqrS2rdiVPl/s2uC/CY=</pin>
        </pin-set>
    </domain-config>

    <!-- Local development: allow cleartext to emulator (AVD) and USB-forwarded port -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>

    <!-- Default: HTTPS only, no user-installed CAs in production -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

</network-security-config>
