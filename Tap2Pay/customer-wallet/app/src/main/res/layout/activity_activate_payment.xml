<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="32dp"
    android:background="#1A1A2E">

    <!-- App / brand icon placeholder -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="OrchestratePay"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#E0E0E0"
        android:layout_marginBottom="48dp"/>

    <!-- Dynamic status message: "Tap when ready" / "Hold to POS" -->
    <TextView
        android:id="@+id/tv_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tap when you're ready to pay"
        android:textSize="18sp"
        android:textColor="#B0BEC5"
        android:gravity="center"
        android:layout_marginBottom="32dp"/>

    <!-- 60-second countdown — hidden until session is active -->
    <TextView
        android:id="@+id/tv_countdown"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="60"
        android:textSize="72sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:visibility="gone"
        android:layout_marginBottom="16dp"/>

    <!-- Spinner shown while fetching token from backend -->
    <ProgressBar
        android:id="@+id/progress_loading"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:visibility="gone"
        android:layout_marginBottom="24dp"/>

    <!-- Primary CTA — hidden once a session is active -->
    <Button
        android:id="@+id/btn_activate"
        android:layout_width="240dp"
        android:layout_height="56dp"
        android:text="Ready to Pay"
        android:textSize="16sp"
        android:backgroundTint="#4CAF50"
        android:textColor="#FFFFFF"
        android:layout_marginTop="16dp"/>

</LinearLayout>
